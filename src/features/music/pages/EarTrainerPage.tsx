import { EarTrainer } from '../components/EarTrainer/EarTrainer';

export default function EarTrainerPage() {
    return (
        <div className="min-h-screen bg-slate-50 py-6">
            <EarTrainer />
        </div>
    );
}
